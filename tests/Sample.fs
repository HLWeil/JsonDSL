module JsonDSLTests.StringCreation

open JsonDSL
open Expecto

[<Tests>]
let ``json string creation tests`` =
    testList "json string creation" [
        testCase "" (fun _ ->
            ()
        )
    ]
